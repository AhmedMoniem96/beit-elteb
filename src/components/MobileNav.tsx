"use client"; import {useState} from "react"; import Link from "next/link"; import {copy,Locale} from "@/lib/i18n";
export function MobileNav({locale}:{locale:Locale}) { const [open,setOpen]=useState(false), t=copy[locale], other=locale==="en"?"ar":"en"; const links=[["services",t.nav.services],["doctors",t.nav.doctors],["about",t.nav.about],["insurance",t.nav.insurance],["contact",t.nav.contact]]; return <><button className="menu" aria-expanded={open} aria-label="Menu" onClick={()=>setOpen(!open)}>☰</button><nav className={open?"nav open":"nav"} aria-label="Main navigation">{links.map(([p,l])=><Link key={p} href={`/${locale}/${p}`} onClick={()=>setOpen(false)}>{l}</Link>)}<Link href={`/${other}`}>{other.toUpperCase()}</Link><Link className="button small" href={`/${locale}/book-appointment`}>{t.nav.book}</Link></nav></> }
