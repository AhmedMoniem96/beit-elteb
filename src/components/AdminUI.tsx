import Link from "next/link";
export function AdminPageHeader({ title, description, action }: { title: string; description: string; action?: { href: string; label: string } }) { return <div className="admin-page-header"><div><h1>{title}</h1><p>{description}</p></div>{action && <Link className="admin-primary" href={action.href}>{action.label}</Link>}</div>; }
export function AdminCard({ children, title, subtitle, className = "" }: { children: React.ReactNode; title?: string; subtitle?: string; className?: string }) { return <section className={`admin-card ${className}`}>{title && <div className="admin-card-head"><div><h2>{title}</h2>{subtitle && <p>{subtitle}</p>}</div></div>}{children}</section>; }
export function StatCard({ label, value, icon, subtitle }: { label: string; value: number; icon: string; subtitle?: string }) { return <article className="admin-stat-card"><span className="admin-stat-icon" aria-hidden>{icon}</span><div><p>{label}</p><strong>{value}</strong>{subtitle && <small>{subtitle}</small>}</div></article>; }
export function StatusBadge({ status }: { status: string | boolean }) { const value = typeof status === "boolean" ? status ? "PUBLISHED" : "DRAFT" : status; return <span className={`admin-badge badge-${value.toLowerCase()}`}>{value.replaceAll("_", " ")}</span>; }
export function EmptyState({ title, description }: { title: string; description: string }) { return <div className="admin-empty"><span aria-hidden>✦</span><h3>{title}</h3><p>{description}</p></div>; }
export function Initials({ name }: { name: string }) { return <span className="admin-initials">{name.split(" ").map(x => x[0]).slice(0, 2).join("").toUpperCase()}</span>; }
export function FormSection({ title, description, children }: { title: string; description: string; children: React.ReactNode }) { return <section className="admin-card admin-form-section"><div className="admin-form-intro"><h2>{title}</h2><p>{description}</p></div><div className="admin-form-grid">{children}</div></section>; }
