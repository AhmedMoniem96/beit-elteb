"use client";import {useState} from "react";export function StatusSelect({id,value,type,options}:{id:string;value:string;type:"appointments"|"contacts";options:string[]}){const [status,setStatus]=useState(value),[saving,setSaving]=useState(false),[error,setError]=useState("");return <span><select disabled={saving} className={`admin-status-select badge-${status.toLowerCase()}`} aria-label="Status" value={status} onChange={async e=>{const next=e.target.value,previous=status;setStatus(next);setSaving(true);setError("");try{const res=await fetch(`/api/admin/${type}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,status:next})});if(!res.ok)throw new Error();}catch{setStatus(previous);setError("Update failed")}finally{setSaving(false)}}}>{options.map(o=><option key={o}>{o}</option>)}</select>{error&&<small className="admin-inline-error" role="status">{error}</small>}</span>}
