import {db} from "@/lib/db";import {session} from "@/lib/auth";import {redirect} from "next/navigation";import {AdminPageHeader} from "@/components/AdminUI";import {MessageTable} from "@/components/MessageTable";export const dynamic="force-dynamic";export default async function Page(){if(!await session())redirect("/admin/login");const rows=await db.contactMessage.findMany({orderBy:{createdAt:"desc"}});return <><AdminPageHeader title="Messages" description="View and manage patient enquiries."/><MessageTable messages={rows.map(x=>({...x,createdAt:x.createdAt.toISOString()}))}/></>}
