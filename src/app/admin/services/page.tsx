import {db} from "@/lib/db";import {session} from "@/lib/auth";import {redirect} from "next/navigation";import {ServiceManager} from "@/components/ServiceManager";import {AdminPageHeader} from "@/components/AdminUI";export const dynamic="force-dynamic";export default async function Page(){if(!await session())redirect("/admin/login");const items=await db.service.findMany({orderBy:{createdAt:"desc"}});return <><AdminPageHeader title="Services" description="Manage healthcare services shown across the website."/><ServiceManager initial={items}/></>}
