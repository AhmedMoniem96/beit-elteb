import {db} from "@/lib/db";import {session} from "@/lib/auth";import {redirect} from "next/navigation";import {AdminPageHeader} from "@/components/AdminUI";import {DoctorsTable} from "@/components/DoctorsTable";export const dynamic="force-dynamic";export default async function Page(){if(!await session())redirect("/admin/login");const rows=await db.doctor.findMany({include:{specialty:true},orderBy:{sortOrder:"asc"}});return <><AdminPageHeader title="Doctors" description="Manage medical staff and their public profiles." action={{href:"/admin/doctors/new",label:"＋ Add doctor"}}/><DoctorsTable initial={rows.map(x=>({...x,consultationPrice:x.consultationPrice?.toString(),updatedAt:x.updatedAt.toISOString()}))}/></>}
