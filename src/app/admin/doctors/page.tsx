import Link from "next/link";import {db} from "@/lib/db";import {session} from "@/lib/auth";import {redirect} from "next/navigation";import {AdminPageHeader,EmptyState,Initials,StatusBadge} from "@/components/AdminUI";export const dynamic="force-dynamic";
export default async function Page(){if(!await session())redirect("/admin/login");const rows=await db.doctor.findMany({include:{specialty:true},orderBy:{sortOrder:"asc"}});return <><AdminPageHeader title="Doctors" description="Manage medical staff and their public profiles." action={{href:"/admin/doctors/new",label:"＋ Add doctor"}}/><div className="admin-toolbar admin-card"><input aria-label="Search doctors" placeholder="⌕  Search doctors…"/><select aria-label="Specialty filter"><option>All specialties</option></select><select aria-label="Status filter"><option>All statuses</option><option>Published</option><option>Draft</option></select><select aria-label="Featured filter"><option>All doctors</option><option>Featured</option></select></div><section className="admin-card">{rows.length?<><div className="admin-table-wrap"><table className="admin-table desktop-only"><thead><tr><th>Doctor</th><th>Specialty</th><th>Status</th><th>Featured</th><th>Order</th><th>Updated</th><th>Actions</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><div className="admin-person">{x.imageUrl?<img className="admin-thumb" src={x.imageUrl} alt=""/>:<Initials name={x.nameEn}/>}<div><strong>{x.nameEn}</strong><small dir="rtl">{x.nameAr} · {x.titleAr}</small></div></div></td><td>{x.specialty.nameEn}</td><td><StatusBadge status={x.published}/></td><td>{x.featured?"Yes":"—"}</td><td>{x.sortOrder}</td><td>{x.updatedAt.toLocaleDateString()}</td><td><div className="admin-row-actions"><Link className="admin-action" href={`/admin/doctors/${x.id}`}>Edit</Link><Link className="admin-action" href={`/en/doctors/${x.slug}`} target="_blank">View ↗</Link></div></td></tr>)}</tbody></table></div><div className="admin-mobile-cards">{rows.map(x=><article className="admin-mobile-card" key={x.id}><div className="admin-person">{x.imageUrl?<img className="admin-thumb" src={x.imageUrl} alt=""/>:<Initials name={x.nameEn}/>}<div><strong>{x.nameEn}</strong><small>{x.specialty.nameEn}</small></div></div><div><StatusBadge status={x.published}/><Link className="admin-action" href={`/admin/doctors/${x.id}`}>Edit →</Link></div></article>)}</div></>:<EmptyState title="No doctors yet" description="Add your first doctor profile to get started."/>}</section></>}
