import {db} from "@/lib/db";import {session} from "@/lib/auth";import {redirect} from "next/navigation";import {AdminPageHeader} from "@/components/AdminUI";import {DoctorForm} from "@/components/DoctorForm";export const dynamic="force-dynamic";export default async function Page(){if(!await session())redirect("/admin/login");const specialties=await db.specialty.findMany({orderBy:{nameEn:"asc"}});return <><AdminPageHeader title="Add doctor" description="Create a complete public profile for a medical team member."/><DoctorForm specialties={specialties}/></>}
