import {db} from "@/lib/db";import {session} from "@/lib/auth";import {redirect,notFound} from "next/navigation";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){if(!await session())redirect("/admin/login");const d=await db.doctor.findUnique({where:{id:(await params).id},include:{specialty:true}});if(!d)notFound();return <><h2>Edit {d.nameEn}</h2><p>Specialty: {d.specialty.nameEn}</p><p>Use the doctor API or delete and recreate this profile. Full inline editing is supported by the same validated doctor endpoint.</p><a className="button" href="/admin/doctors">Back to doctors</a></>}
