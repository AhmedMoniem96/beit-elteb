import {db} from "@/lib/db";import {session} from "@/lib/auth";import {redirect,notFound} from "next/navigation";import {AdminPageHeader} from "@/components/AdminUI";import {DoctorForm} from "@/components/DoctorForm";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){if(!await session())redirect("/admin/login");const [doctor,specialties]=await Promise.all([db.doctor.findUnique({where:{id:(await params).id}}),db.specialty.findMany({orderBy:{nameEn:"asc"}})]);if(!doctor)notFound();return <><AdminPageHeader title={`Edit ${doctor.nameEn}`} description="Update profile information, professional details and publishing options."/><DoctorForm specialties={specialties} doctor={{...doctor,consultationPrice:doctor.consultationPrice?.toString()}}/></>}
