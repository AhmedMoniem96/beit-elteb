import {db} from "@/lib/db";import {session} from "@/lib/auth";import {redirect} from "next/navigation";import {AdminPageHeader} from "@/components/AdminUI";import {AppointmentsTable} from "@/components/AppointmentsTable";export const dynamic="force-dynamic";export default async function Page(){if(!await session())redirect("/admin/login");const rows=await db.appointment.findMany({include:{service:true,doctor:true,specialty:true},orderBy:{preferredAt:"asc"}});return <><AdminPageHeader title="Appointments" description="Review patient bookings and coordinate your clinical schedule."/><AppointmentsTable initial={rows.map(x=>({...x,preferredAt:x.preferredAt.toISOString(),createdAt:x.createdAt.toISOString(),updatedAt:x.updatedAt.toISOString(),doctor:x.doctor&&{id:x.doctor.id,nameEn:x.doctor.nameEn},specialty:x.specialty&&{id:x.specialty.id,nameEn:x.specialty.nameEn},service:x.service&&{titleEn:x.service.titleEn}}))}/></>}
