import {db} from "@/lib/db";import {session} from "@/lib/auth";import {redirect} from "next/navigation";import {AdminCard,AdminPageHeader,EmptyState} from "@/components/AdminUI";export const dynamic="force-dynamic";export default async function Page(){if(!await session())redirect("/admin/login");const media=await db.media.findMany({orderBy:{createdAt:"desc"}});return <><AdminPageHeader title="Media Library" description="Upload and reuse approved visual assets across your website."/><AdminCard title="Upload media" subtitle="Add a new image to your secure Supabase library"><form className="admin-media-upload" action="/api/admin/media" method="post" encType="multipart/form-data"><span className="admin-stat-icon" style={{margin:"0 auto 10px"}}>↑</span><strong>Drag and drop an image here</strong><p className="admin-cell-muted">PNG, JPG, WEBP or SVG · Maximum file size 5MB</p><input type="file" name="file" accept="image/*" required/><div className="admin-form-grid" style={{marginTop:18}}><label className="admin-field">English alt text<input name="altEn"/></label><label className="admin-field">Arabic alt text<input name="altAr" dir="rtl"/></label></div><button style={{marginTop:16}}>Upload image</button></form></AdminCard>{media.length?<div className="admin-media-grid">{media.map(x=><article className="admin-card admin-media-item" key={x.id}><img src={x.url} alt={x.altEn||"Uploaded media"}/><div className="admin-media-meta"><strong>{x.path.split("/").pop()}</strong><p>{x.path}</p><p>EN: {x.altEn||"No alt text"}</p><p dir="rtl">AR: {x.altAr||"لا يوجد"}</p><input readOnly value={x.url} aria-label="Reusable URL" onFocus={undefined}/></div></article>)}</div>:<EmptyState title="Your media library is empty" description="Upload an image to start building your asset library."/>}</>}
