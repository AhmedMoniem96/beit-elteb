import {db} from "@/lib/db";import {session} from "@/lib/auth";import {redirect} from "next/navigation";export const dynamic="force-dynamic";export default async function Page(){if(!await session())redirect("/admin/login");const media=await db.media.findMany({orderBy:{createdAt:"desc"}});return <><h2>Media library</h2><form className="form" action="/api/admin/media" method="post" encType="multipart/form-data"><label>Image (max 5MB)<input type="file" name="file" accept="image/*" required/></label><label>English alt text<input name="altEn"/></label><label>Arabic alt text<input name="altAr" dir="rtl"/></label><button>Upload securely</button></form><div className="grid">{media.map(x=><article className="card" key={x.id}><img src={x.url} alt={x.altEn||"Uploaded media"} style={{maxWidth:"100%"}}/><input readOnly value={x.url} aria-label="Reusable URL"/></article>)}</div></>}
