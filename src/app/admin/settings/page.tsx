import {db} from "@/lib/db";import {session} from "@/lib/auth";import {redirect} from "next/navigation";import {AdminPageHeader} from "@/components/AdminUI";import {SettingsForm} from "@/components/SettingsForm";export const dynamic="force-dynamic";export default async function Page(){if(!await session())redirect("/admin/login");const s=await db.siteSettings.findUnique({where:{id:"default"}});const settings=s?Object.fromEntries(Object.entries(s).filter(([key])=>!["id","updatedAt"].includes(key))) as Record<string,string|null>:{siteName:"Beit El Teb"};return <><AdminPageHeader title="Settings" description="Manage clinic identity, contact details and search metadata."/><SettingsForm settings={settings}/></>}
