import {BookingForm} from "@/components/BookingForm";import {db} from "@/lib/db";import {isLocale} from "@/lib/i18n";import {notFound} from "next/navigation";export const dynamic="force-dynamic";
export default async function Book({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const [specialties,doctors,services]=await Promise.all([db.specialty.findMany({where:{published:true},select:{id:true,nameEn:true,nameAr:true},orderBy:{sortOrder:"asc"}}),db.doctor.findMany({where:{published:true},select:{id:true,nameEn:true,nameAr:true,specialtyId:true},orderBy:{sortOrder:"asc"}}),db.service.findMany({where:{published:true},select:{id:true,titleEn:true,titleAr:true}})]);return <section className="section container"><div className="page-head"><span className="eyebrow">{locale==="ar"?"خطوات بسيطة":"SIMPLE, PERSONAL BOOKING"}</span><h1>{locale==="ar"?"احجز موعدك":"Book your appointment"}</h1><p>{locale==="ar"?"اختر التخصص والطبيب والوقت المناسب، وسنؤكد موعدك قريباً.":"Choose the right specialist and a convenient time. Our team will confirm shortly."}</p></div><BookingForm locale={locale} specialties={specialties} doctors={doctors} services={services}/></section>}
