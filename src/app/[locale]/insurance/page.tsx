import {db} from "@/lib/db";import {isLocale} from "@/lib/i18n";import {notFound} from "next/navigation";export const dynamic="force-dynamic";
export default async function Insurance({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const rows=await db.insuranceProvider.findMany({where:{published:true},orderBy:{sortOrder:"asc"}});return <section className="section container"><div className="page-head"><span className="eyebrow">{locale==="ar"?"شركاؤنا":"OUR PARTNERS"}</span><h1>{locale==="ar"?"رعاية ضمن تغطيتك":"Care within your coverage"}</h1><p>{locale==="ar"?"نتعاون مع أبرز شركات التأمين لتسهيل تجربتك.":"We partner with leading insurers to make exceptional care easier to access."}</p></div><div className="partner-grid">{rows.map(x=><article className="partner" key={x.id}>{x.logoUrl?<img src={x.logoUrl} alt={locale==="ar"?x.nameAr:x.nameEn}/>:<span>{locale==="ar"?x.nameAr:x.nameEn}</span>}<p>{locale==="ar"?x.descriptionAr:x.descriptionEn}</p></article>)}</div></section>}
