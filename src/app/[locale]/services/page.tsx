import Link from "next/link";import {db} from "@/lib/db";import {isLocale} from "@/lib/i18n";import {notFound} from "next/navigation";export const dynamic="force-dynamic";
export default async function Services({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const items=await db.service.findMany({where:{published:true},orderBy:{sortOrder:"asc"}});return <section className="section container"><div className="page-head"><span className="eyebrow">{locale==="ar"?"خدماتنا":"OUR SERVICES"}</span><h1>{locale==="ar"?"رعاية متكاملة مصممة لك":"Connected care, designed around you"}</h1></div><div className="service-grid">{items.map(s=><Link href={`/${locale}/services/${s.slug}`} className="service-card" key={s.id}><span className="service-icon">{s.icon||"✦"}</span><h2>{locale==="ar"?s.titleAr:s.titleEn}</h2><p>{locale==="ar"?s.descriptionAr:s.descriptionEn}</p><b>{locale==="ar"?"التفاصيل":"View service"} →</b></Link>)}</div></section>}
